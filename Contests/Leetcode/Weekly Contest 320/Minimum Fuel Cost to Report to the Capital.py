class Solution:
    def minimumFuelCost(self, roads: List[List[int]], seats: int) -> int:
        # roads = n and cities = n+1
        # city capital is city 0
        prime_digs = (2,3,5,7)
        if not roads: return 0
        min_liters = 0
        return min_liters
