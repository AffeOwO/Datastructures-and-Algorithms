#include <bits/stdc++.h>

//ifstream fin("");
//ofstream fout("");

using namespace std;
using ll = long long;
using ull = unsigned long long;
using ld = long double;
#define endl "\n"
#define all(x) (x).begin(), (x).end()
#define MOD 1000000007

ll n, c; // cnt is initial cnt of c in nums
vector<ll> nums;

void solve() {
    cin >> n >> c;
    nums.resize(n); 
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();   
}